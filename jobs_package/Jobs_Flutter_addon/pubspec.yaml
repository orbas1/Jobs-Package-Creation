name: jobs_flutter_addon
version: 0.1.0
publish_to: "none"
description: "Flutter addon that mirrors the Jobs Laravel package for mobile clients."
author: "Jobi Maintainers <maintainers@jobi.test>"
environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  intl: ^0.19.0
  collection: ^1.18.0
  meta: ^1.11.0
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^3.0.0

flutter:
  uses-material-design: true
